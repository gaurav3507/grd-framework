# GRD Framework Track Sheet

One row per produced artefact. Fill commit_hash after the commit that produced the row.

| date | machine (Mac/A100) | script | produced_artefact | commit_hash | notes |
|------|--------------------|--------|-------------------|-------------|-------|
| 2026-09-01 | Mac | (none) | M0 scaffolding + design doc | ca2e66b | repo created |
| 2026-09-08 | Mac | sim/simulator.py, src/gate/rank_readout.py, experiments/e0_oracle.py | results/e0/e0_report.json (e0_spectra.npz untracked) | 278dd98 | M1 simulator port + E0 oracle green on Mac CPU; E0-A/E0-B/E0-C all PASS across seeds 0-9; venv .venv with numpy/scipy/scikit-learn |
| 2026-09-09 | Mac | src/recover/backbone.py, experiments/e1_recover_check.py | results/e1/e1_recover_report.json | 2e93c78 | M2 recover backbone. Bing "ours" (external/, .venv-recover) ran but did not recover on our variance-changing interventions (MCC 0.63, DO-control 0.999 proves estimator ok); adopted design-doc R2 fallback: covariance/precision-difference linear estimator, pure numpy/scipy, no torch, runs in .venv. E1 recovery MCC 0.999 all seeds 0-9 vs E0-C blind 0.42-0.75 |
| 2026-09-09 | Mac | experiments/e2p5_regime_coupling.py | results/e2p5/regime_coupling_report.json | e26403c | M2.5 gate-backbone regime coupling (gates M3). STATUS=FAIL by design (exits 1). Q1 FAILED: P4 gate does NOT read per-env atomic rank cleanly in the reducing regime (LFC k_hat correct 5/10 seeds) nor increasing (7/10); misreads are +/-1 at low sv-gap. Backbone recovers reducing only (MCC 0.999) not increasing (mean 0.79). NOT cleanly coupled: backbone's only working regime (reducing) is the gate's WEAKER regime. M3 must not assume the gate can vet reducing-regime data at per-env resolution |
| 2026-09-10 | Mac | experiments/e2p6_aggregate_gate.py | results/e2p6/aggregate_gate_report.json | 322e6f0 | M2.6 aggregate-resolution gate check (decides M3 fork). STATUS=FAIL by design (exits 1). Aggregation reuses E0 exact method (single all-node hard env, covariance_difference + estimate_rank_lfc at d_proj=d_latent; no pooling fn exists). Aggregate LFC reads n_recoverable=d correctly 9/10 reducing seeds but STABLY under-reads seed 7 (k_hat 4 vs true 5, boundary reject rate 0/10 boot): weak aggregate direction (sv 1.1 vs dominant 97-1395) masked by dominant directions sampling fluctuation; backbone still recovers it (MCC 0.9996) via per-node precision. GATE-BACKBONE DISAGREEMENT: gate=aggregate covariance rank, backbone=per-node precision, different signal representations. spectral_rank under-reads (1-4) always. Path A not cleanly supported |
| 2026-09-10 | Mac | src/gate/precision_readout.py, experiments/e2p7_precision_gate_coupling.py | results/e2p7/precision_gate_report.json | 8dbcf65 | Path A FIX: new per-node precision gate readout reads the SAME signal the backbone uses (top eigenvalue of per-node precision diff inv(cov(Y_env))-inv(cov(Y_obs)) vs a control-vs-control null; reuses alpha 0.05 / B 500, no new knob). Recheck STATUS=PASS: new gate count == true dim d=5 on ALL seeds 0-9 (min margin 9.9-31.6x over null); old pooled-covariance gate failed only seed 7 (read 4), new gate fixes it (reads 5). Gate and backbone now agree on all seeds. Resolves the M2.6 disagreement |
| 2026-09-10 | Mac | src/discover/discover.py, experiments/e1_full_pipeline.py | results/e1_full/e1_full_report.json | 6983a7a | M3 discover module + first full pipeline. Discover: interventional order id (perfect interventions reveal descendants) + ordered OLS linear-SEM regression + orientation-checked abstention (EDGE_DECIDED_PRESENT/ABSENT, EDGE_UNDECIDED_ORDER/WEAK); standard methods, no new algorithm. E1-full (GATE precision -> RECOVER backbone -> DISCOVER) STATUS=PASS all seeds 0-9: (a) gate n_recoverable==5, (b) recovery MCC 0.9988-0.9996, (c) SHD over decided edges==0, decided precision/recall 1.00, abstention 0.00-0.50. (c) bar: SHD_decided==0 AND >=1 edge decided present |
| 2026-09-10 | Mac | experiments/e2_calibration.py | results/e2/e2_calibration_report.json | 709f5f2 | E2 headline: violation-injection calibration, 4 arms x 5 severities x seeds 0-9, gate verdict vs naive-backbone MCC. STATUS=PASS, central claim SUPPORTED. Pre-registered all 4 arms; all held. A env/direction-count starvation: TRACKS_TIGHT (gate n_rec=m exactly, naive MCC lockstep 0.999->0.20, crossover at m=4, cert recovery 0.999). B power (n_e): TRACKS_CONSERVATIVE (gate caps at n_e=40 before sample-robust naive fails; no silent PROCEED). D weak-signal (iv->1): TRACKS_CONSERVATIVE (caps at iv=0.7 before naive falls at 0.9); node-count starvation coincides with A in atomic construction so D realized as intervention-strength. C measurement contamination: FOOLED (detectability-based NOT P3; certifies non-recoverable measurement directions, cert recovery 0.999->0.74) - the pre-registered P3 gap (R1, temporal-only, deferred). No arm proceeds while naive collapses. Not killed (A strongly calibrated) |
| 2026-09-14 | Mac | src/gate/precision_readout.py, experiments/e3p0_null_sizecheck.py | results/e3p0/null_sizecheck_report.json | 2847579 | E3 fix (Lesson 2): sample-size-matched null in precision gate. Bug: null_threshold bootstrapped at Y_obs size but precision_signal called on smaller envs -> inflated detection. Fix: null_threshold gains n_env (resample size); count_recoverable computes a per-env size-matched null (n_env=len(Y)); n_env=None preserves old behaviour exactly. Backward-compat re-verified: E2.7 Path A and E1-full STILL PASS unchanged (verdicts/counts/SHD identical; only e2p7 min_ratio diagnostic shifts ~1-2pct from per-env null MC realization). E2 re-verified PASS, same central claim/behaviors (borderline per-seed verdict shifts at detection boundary; arm D tightness label at calibration_gap=0.6 boundary). New regression test e3p0: fake small pure-control envs false-detect at 6.7pct (size-matched null, near alpha) vs 95.7pct (old full-size null=bug). Milestone report snapshots left as historical (re-runs were verification only) |
| 2026-09-15 | Mac (write) / A100 (run) | experiments/ilcs_baseline.py | results/ilcs_baseline/ (deferred to A100) | 8fc9aa3 | iLCS ungated baseline (Chen et al. 2024 arXiv 2410.24059 Algorithm 1, reimplemented; original repo github.com/TianyuCodings/iLCS is 404). Runs iLCS on the SAME D=10 control-fit projected E3 inputs as the precision gate. Two arms per env: NAIVE alpha=0.2 (paper default d<=10), CALIBRATED size-matched null (B=20, 95th pct of max_i L over control-vs-control draws, Lesson 2). Arms per dataset: 50 pure-control fakes, 20 random splits, 20 structured control-PC splits, all powered perts (NMIN=200); K562/RPE1 single=False, Norman single-gene. Reuses panel pr-loader + proj recipe and confound struct-split verbatim; modifies no e3 script. Local synthetic self-test PASS: naive fires on pure sampling noise, calibrated does not. Data on A100 not Mac so smoke/full deferred to A100. jaccard_calib_vs_gate=null: committed gate JSONs carry only aggregate counts, no per-pert detect dict |
| 2026-09-16 | Mac (write) / A100 (run) | src/gate/precision_readout.py; E3 panel/stability/confound/positive-control scripts | corrected-null and BH-FDR E3 results | 61d6b50, ffed4d7 | Disjoint small-environment null plus BH-FDR. Corrected BH detections: K562 0/385, RPE1 48/146, Norman 0/101, HCP 5/6, ABIDE 13/13. Random RPE1 controls 0/20; structured splits 20/20. |
| 2026-09-17 | Mac (write) / A100 (run) | experiments/e3_attribution_panel.py | results/e3_attribution/panel_attribution.json | 37178b7 | Full-dimensional real-data subspace attribution was inconclusive: RPE1 0/48 BH rejections and HCP 0/5. Result is non-rejection, not mechanism confirmation. |
| 2026-09-18 | Mac | experiments/e2d_attribution_power_gate.py | results/e2d_attribution/projected_attribution_report.json | c034017 | Predeclared projected-attribution rescue returned NO_GO: RPE1-like mixed-gain power 0.85 but mechanism false attribution 0.50; small-n mixed-gain power 0.40. No real-data rescue run. |
| 2026-09-18 | Mac | experiments/e2c_starvation.py | results/e2c/starvation_report.json | 413a8c3 | Non-tautological full-rank starvation estimator. MCC 0.9991, 0.9908, 0.9461, 0.8806, 0.7986 for m=5..1; gate caps at m=4 and MCC crosses 0.90 at m=2, so the gate is conservative rather than exactly calibrated. |
| 2026-09-18 | Mac | experiments/e1_capped_pipeline.py | results/e1_capped/capped_report.json | 6702bd2 | Genuine k=3<d=5 selective pipeline, 10/10 seeds PASS: mean MCC 0.9990, decided-edge SHD 0, all 140 out-of-scope ordered pairs abstained, no out-of-scope commitment. Fixed chain with ancestral-closed supplied subset isolates pipeline mechanics. |
| 2026-09-18 | Mac | experiments/data_paths.py and real-data scripts | portable real-data path resolution | e1c924b | GRD_DATA_ROOT portability with legacy defaults; explicit HCP and ABIDE overrides documented. Existing results not rewritten. |
| 2026-09-18 | Mac | experiments/export_gate_decisions.py, write_data_manifest.py, reproduce_e3.py, ilcs_baseline.py | results/e3_decisions/*.json | 3e74814 | Deterministic corrected-BH decision exports with source SHA-256, external-data manifest generator, one-command E3/iLCS regeneration, and iLCS Jaccard refresh from existing per-seed records. |
